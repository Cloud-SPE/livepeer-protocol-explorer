# Tech Debt Tracker

Known shortcuts, deferred work, and TODOs that are too small to warrant their own plan but should not be lost.

| ID | Item | Source | Severity | Status |
|---|---|---|---|---|
| TD-002 | All SPEC §22 open data items (Q-OD-1 through Q-OD-10) marked `TODO(Q-OD-N)` in code/config — not yet resolved. | SPEC §22 | medium | **Resolved.** All 10 open data items closed 2026-04-27 — see design-docs. |
| TD-003 | `TransferBond` and `WithdrawFees` events appear in the SQLite seed but were not enumerated in SPEC §6.3. | sqlite-seed-mapping.md | medium | **Resolved** in SPEC v1.1 — added to §6.2 / §6.3 with classifications: `TransferBond` LPT-valued strict, `WithdrawFees` ETH-valued non-strict. |
| TD-004 | `events.payload` cross-check pass — verify that every RPC-derived event at `(tx_hash, log_index)` matches the SQLite payload field-by-field. Catches indexer/decoder bugs by comparing against a second decoder (the historical SQLite). | sqlite-seed-mapping.md | medium | **Promoted to v1 acceptance criterion** (SPEC v1.1, §24.1). Implementation: a `livepeer-test cross-check` binary that runs after backfill and writes a discrepancy report. **Real-data finding (S6.1):** the SQLite `events` table has duplicate rows for some on-chain logs — block 456740385 has 2 rows for 1 chain log. Cross-check must dedupe SQLite by `(tx_hash, payload.log_index)` before comparing. |
| TD-005 | Reorg watcher v1 (S7.1) marks rows non-canonical and writes a `reorg_events` audit row, but does not yet update `block_number` / `block_hash` to the new canonical values nor write `reorg_mutations` rows (SPEC §9.3). Multi-block-depth tracking also deferred — v1 records each diverged block as a separate `reorg_events` row with depth=1. | S7.1 | medium | Open. Required for full §9 compliance; non-blocking for indexer v1 since the indexer's next batch will fetch the canonical events anyway and ON CONFLICT DO NOTHING leaves stale rows in `is_canonical=FALSE` state — queryable via `?include_reorged=true`. |
| TD-006 | Valuator's degraded-path version stamp uses a hardcoded `"v1"` prefix concatenated with `_degraded_spot_pre_cardinality`. When v2 valuation versions land, derive the prefix from the operator-passed `--version`. | S8.2.b | low | Open. v1-only blocker; defer until v2 versions are introduced. |
| TD-008 | Finality watcher v1.5 uses a timestamp heuristic (`l1_latest − 600s` for `l1_posted`; `l1_finalized − 60s` for `finalized`) instead of decoding `SequencerInbox.SequencerBatchDelivered` events on L1 and mapping batches to L2 block ranges. The heuristic is conservative (won't mark prematurely if L1 is healthy) but isn't SPEC §9.1-precise. Real implementation needs L1 archive depth for back-fill from Livepeer's Arbitrum genesis (Feb 2022) — user has Chainstack Eth L1 archive available. | S7.2 | medium | Open. Heuristic is operationally correct; precise tracking is a v1.5 refinement. |
| TD-009 | Three post-indexer stages originally used row-by-row patterns where bulk SQL would be orders of magnitude faster. The full set of bulk refactors has now shipped: seed pass (`INSERT … SELECT`), finality watcher sargability + partial index, on-chain ETH/LPT/multi-asset batching, and staker exact/pending refresh bulk fetch + bulk update. The remaining sustained-load issue on the LPT pass is no longer SQL-shape-bound; it is tracked separately under TD-011 as an RPC / connection-pool pathology. | S8.1, S7.2, S9.2 | medium | **Resolved** on 2026-05-04. Runtime follow-up moved to TD-011. |
| TD-010 | API full-history slow-query paper cuts surfaced at ~2.6M raw events / ~1.1M valuations. The identified hotspots have since been addressed by route rewrites and targeted indexes: `/events?sort=amount_usd_desc` (migration `018` + top-N route rewrite), `/valuations` and valuation-backed aggregations (migrations `019` / `020`), `/backfills/status` (approximate counts from `pg_class.reltuples`), and the newer transcoder routes (migration `021`). Empirical re-check on the live dataset put the optimized routes in the ~1ms–350ms range, with `/transcoders/{addr}/delegators/block/{block}` at ~42ms and `/backfills/status` at ~1ms. | S10.1, S10.2, S10.3 | low | **Resolved** on 2026-05-04. Future API performance work is ordinary regression monitoring, not known design debt. |
| TD-011 | LPT on-chain pass throughput collapses under sustained load — 333 events/min for 5-15 min, then drops to <10/min indefinitely while Chainstack itself remains healthy (verified via concurrent curl). Four code-side changes during the 2026-04-29 investigation (within-event try_join!, cross-event JoinSet at N=14, 1-shot HTTP-error retry, dead-band skip) all preserve determinism and helped fresh-cache throughput, but none fixed the sustained-load collapse. HTTP/2 keep-alive made it worse. Hypothesis: Cloudflare-side connection-tracker demoting long-lived flows from our IP, or reqwest pool entering a wedged state with HTTP/2 multiplexing. Full investigation context, ruled-out hypotheses, repro commands, and three suggested next experiments (periodic pool refresh / force HTTP/1.1 + connection-close / second archive provider) in [td-011-lpt-onchain-throughput.md](active/td-011-lpt-onchain-throughput.md). State at last kill: 39,942 LPT on-chain valuations, latest priced block 22,067,586 of 457,212,919. Resume is clean — candidate-skip filter picks up where it left off. | core/rpc, livepeer-valuator | medium | Open. Backfill is paused; correctness/determinism unaffected; the LPT-pass-API-coverage gap is for blocks 22.07M → head. |
| TD-012 | Daemon mode — keep the pipeline at chain head without operator-driven re-launches. The implementation is now in progress: `livepeer-orchestrator` ships `bootstrap` / strict-by-default `replay`; `livepeer-daemon` ships `follow` with shared RPC handles, a process-wide RPC concurrency ceiling, per-task soft RPC caps, coordinated shutdown, bounded indexer/finality/reorg/valuator/staker loops, and a Prometheus `/metrics` + `/health` surface with live indexed/decode/valuation/reorg counters plus provider-level RPC metrics. Strict replay determinism CI is now real via committed multi-case fixtures plus `scripts/run-determinism-replay.sh` and `.github/workflows/determinism.yml`. Repo-managed alerting scaffold now also exists: Prometheus rules, Alertmanager webhook config, and `livepeer-alert-bot` for Telegram delivery. Remaining work is production hardening: explicit provider failover policy once a second archive exists, full production alert wiring validation, and operator polish for the daemonized path. Full phase breakdown in [td-012-daemon-mode.md](active/td-012-daemon-mode.md). | SPEC §13.5, §17.2 | medium | **In progress.** Phase 1 done; Phase 2 landed; Phase 3 hardening is underway as of 2026-05-04. |
| TD-013 | Valuation completion semantics mismatch: a broad query against only `valuation_version = 'v1_lpt_weth_twap_30min_x_chainlink_eth'` reports ~226k valuable finalized rows without `event_valuations`, but most of those rows are actually priced under the degraded version (`v1_degraded_spot_pre_cardinality`). After counting both versions, the true residual is `3,572`, and all 3,572 have terminal `valuation_attempts` (`failed_missing_oracle`, `failed_missing_pool`, `failed_sequencer_outage`) with **0** unattempted rows. Operator chose **Model A** on 2026-05-03: every valuable event must also have an `event_valuations` row. Migration `017_event_valuations_terminal_failures`, valuator write-path changes, replay-DB application, and spec/acceptance wording updates now align on that outcome-row contract. Concrete evidence and implementation notes are tracked in [td-013-valuation-coverage-gap.md](completed/td-013-valuation-coverage-gap.md). | SPEC §1.1, §24.1 | high | **Resolved** on 2026-05-03. Future work is normal reporting/ops polish, not a semantics gap. |
| TD-014 | Gateway/TicketBroker API phase 1 originally shipped exact balance-by-block reads from on-chain `getSenderInfo()` plus indexed flow/summary endpoints, but phase 2 has now landed: historical sender balance snapshots are materialized in `gateway_balances_by_block`, a bounded gateway backfill worker exists under `livepeer-staker`, daemon follow mode runs that backfill, and the API prefers stored rows before live-RPC fallback. Closure details are captured in [td-014-gateway-phase-2.md](completed/td-014-gateway-phase-2.md). | gateway-ticketbroker-data-model.md | low | **Resolved** on 2026-05-04. Remaining gateway work moved to TD-015. |
| TD-015 | Gateway phase 3 extended the model beyond sender balances. Claimant-level reserve state, the materialized `gateway_flows` ledger, recipient analytics, and explicit `net` / `gross` payout semantics are now implemented. Closure details are captured in [td-015-gateway-phase-3.md](completed/td-015-gateway-phase-3.md). | gateway-ticketbroker-data-model.md | low | **Resolved** on 2026-05-05. |
| TD-016 | Gateway historical materialization was monolithic and balance-first: it computed full candidate sets, replayed all gateway balance snapshots with per-candidate RPC reconciliation, and only then began writing `gateway_flows` / `gateway_claimants_by_block`. In practice that left those tables at zero for hours even though the code paths existed. Resolved in two passes: (a) Phases A-D (2026-05-05) split the worker into three independently checkpointed bounded phases (`gateway_flow_backfill`, `gateway_claimant_backfill`, `gateway_balance_backfill`) and re-ordered execution flow → claimant → balance for fastest user-visible payoff; (b) Phase E + restart-test (2026-05-07) added the operability surface: new `livepeer_staker::metrics` module exposes 5 metric families (`gateway_backfill_candidates_remaining`, `..._rows_written_total`, `..._last_processed_block`, `..._iterations_total{phase,result}`, `..._iteration_seconds`) via the daemon's `/metrics` endpoint; "gateway backfill complete" log line now includes `elapsed_ms`; restart-resume validated by SIGKILL'ing an in-flight staker mid-iter and confirming all three checkpoints survived unchanged and the next iter resumed at exact same blocks. Closure details: [td-016-gateway-backfill-operability.md](completed/td-016-gateway-backfill-operability.md). | gateway backfill investigation 2026-05-05 | medium | **Resolved** on 2026-05-07. |
| TD-019 | Staker profile live refresh. `orchestrator_profile` and `broadcaster_profile` were populated only by `livepeer-staker profile-backfill`, a one-shot bounded subcommand. The daemon's `staker_loop` runs flow-backfill, gateway-backfill, and refresh-pending every cycle but did not call `run_profile_backfill`, so once first catch-up finished those profile tables went stale as new TranscoderUpdate / Bond / NewRound events arrived. Resolved by adding a native `livepeer-staker profile-follow` subcommand (default `--cadence-secs 300`) that wraps `run_profile_backfill` in a poll loop matching the rollup `--follow` pattern, plus a `livepeer-staker-profile-follow` service entry in `docker-compose.yml`. Bash wrapper `scripts/loop-staker-profile.sh` deleted; `resume-catchup-all.sh` updated to launch the native subcommand. All TD-017 determinism acceptance criteria preserved (the follow loop only wraps the existing function). Closure details: [td-019-staker-profile-live-refresh.md](completed/td-019-staker-profile-live-refresh.md). | live-mode profile staleness gap surfaced 2026-05-07 | medium | **Resolved** on 2026-05-07. |
| TD-017 | Old API parity + rollup / dimension layer. The legacy `livepeer-backend-rs` API exposes 17 endpoints across orchestrators, broadcasters/gateways, payouts, rewards, and treasury; the new API now covers the full intended TD-017 scope: deterministic profiles, payout/reward/ticket rollups, restored public reads, and the direct-query CSV / ticket / governance-vote routes. The legacy-fee-share inversion was remediated, CSV exports now use cached transaction-receipt gas semantics, reward CSV `eth_price` now correctly reads the stored `pricing_chain.steps` shape, and the final acceptance proof was completed on a bounded live reload window that produced both canonical `Reward` rows and seeded AI gateways in `broadcaster_profile`. The only upstream nuance left is outside TD-017: full live `reorg_mutations` emission still belongs to TD-005. Closure details are captured in [td-017-old-api-parity-and-rollups.md](completed/td-017-old-api-parity-and-rollups.md). | legacy `livepeer-backend-rs` API gap analysis 2026-05-05 | medium | **Resolved** on 2026-05-05. |
| TD-018 | API read-path performance hardening. `/aggregations/events` broad valuation-backed queries fell back to a multi-second raw-event scan (~15.1s on the full-history daily USD shape). Resolved by adding the `event_metrics_daily` rollup (migration 038) plus a new `livepeer-rollups event-metrics-daily` worker, and rewriting the endpoint to dispatch between a rollup-backed path and the original raw-events path. A correctness gate (`rollup_covers_range`) keeps the rollup path off requests whose upper bound exceeds the rollup's checkpoint, so partial backfill never silently returns short data; the response carries an explicit `source` field so callers can see which path served them. Measured: rollup-eligible queries now serve in ~18-35ms (target was <500ms), the raw_events fallback is ~1.3s for the same broad shape (down from ~15s thanks in part to the 037 lateral-lookup index landed earlier the same day). Track B (report-route hardening) and Track C (benchmark CI harness) intentionally deferred per the plan — current shapes are inside their watch thresholds. Closure details: [td-018-api-read-path-performance-hardening.md](completed/td-018-api-read-path-performance-hardening.md). | live API + EXPLAIN analysis 2026-05-07 | medium | **Resolved** on 2026-05-07. |
| TD-027 | Insight APIs + UI screens. After TD-025/TD-026 produced rich historical datasets (`orch_stake_by_round`, two matviews, plus the always-on `tx_receipts`), the API surface and frontend needed extension to expose them. Resolved end-to-end: 6 new endpoints (`/orchestrators/{addr}/stake-history`, `/orchestrators/{addr}/cuts-history`, `/orchestrators/{addr}/net-economics`, `/delegators/{addr}`, `/network/stats`, `/rounds/{round_id}`) under three new OpenAPI tags ("Orchestrator history", "Delegators", "Network"), all backed purely by existing Postgres state. Phase 0 backend extension added `latest_round_started_block` + `latest_round_started_at` to `/network/stats`. Frontend: TS types mirror Rust response structs verbatim; new `network`/`delegators`/`stake-history` service wrappers; dashboard Network-totals card consumes `/network/stats`; orchestrator detail page extended with stake-history chart + cuts-history timeline + net-economics card; new `views/delegator-detail.ts` and `views/round-detail.ts` + routes; `<address-chip kind="...">` prop replaces hardcoded routing; placeholder `/delegators` and `/rounds` index routes shipped (real index endpoints deferred to a follow-up TD). Live-verified on prod deploy 2026-05-09. Closure details: [td-027-insight-apis-and-ui-screens.md](completed/td-027-insight-apis-and-ui-screens.md). | post-TD-025/TD-026 follow-up 2026-05-09 | medium | **Resolved** on 2026-05-09. |
| TD-026 | Orchestrator stake-by-round historical table. The orch fanout in `livepeer-staker profile-follow` was walking every NewRound × 1,936 orchs and reading `transcoderTotalStake` + `getServiceURI` at each — but only keeping the latest snapshot in `orchestrator_profile` (overwrite-on-conflict). Every prior snapshot was discarded. Unlike gateway snapshots (which persist into `gateway_balances_by_block`), orchestrator total-stake history had no dedicated home. Resolved: new table `orch_stake_by_round` with `(chain_id, address, round)` PK (migration 043); migration 044 ports the existing 1,936 `orchestrator_profile` rows into the new historical table and replaces `orchestrator_profile` with a `MATERIALIZED VIEW` over it; migration 045 relaxes the `triggering_event_id` constraint to mirror `gateway_balances_by_block`'s shape. Worker refactored to bulk-INSERT per NewRound via `insert_orch_stake_by_round_batch` (replaces 1,936 individual upserts with a single `push_values` INSERT + `ON CONFLICT DO NOTHING`); non-NewRound writes removed (lifecycle/cuts derive from event tables, no profile-table side effect needed). Daemon's matview-refresh hook (TD-025) extended to refresh both `broadcaster_profile` and `orchestrator_profile` every 30 s. Live cutover verified: 24,736 historical rows captured across 13 rounds in the first 2 min of the new worker; matview returning expected current state via API; both matview refresh metrics ticking. Closure details: [td-026-orch-stake-by-round.md](completed/td-026-orch-stake-by-round.md). | profile-follow throughput investigation 2026-05-08 | medium | **Resolved** on 2026-05-08. |
| TD-025 | Broadcaster profile as derived view. `staker_gateway_profile` was walking ~600 K gateway-touching events to materialize 13 rows that already existed in `gateway_balances_by_block` (a strict superset, also at chain head). The walk was fully redundant — same RPCs, same blocks, same gateways, same cache. Resolved: dropped the `broadcaster_profile` table (migration 042); replaced with a `MATERIALIZED VIEW` over `gateway_balances_by_block` that now exposes two extra fields previously invisible (`reserve_claimed_in_current_round`, `withdraw_round`); deleted the gateway half of `profile.rs` (~200 lines + struct fields); added a 30 s daemon-hosted REFRESH hook with 2 new metric families (`livepeer_matview_refresh_total`, `livepeer_matview_refresh_seconds`). Live cutover verified: matview populated to 50 gateways immediately (vs. 13 in old table), API exposes new fields, daemon refresh hook ticking every 30 s. The 40 h gateway backfill **stopped on cutover.** Closure details: [td-025-broadcaster-profile-derived-view.md](completed/td-025-broadcaster-profile-derived-view.md). | profile-follow throughput investigation 2026-05-08 | medium | **Resolved** on 2026-05-08. |
| TD-024 | Determinism fixture stale post-TD-017. The committed replay fixtures (`tests/fixtures/case-{a,b}/`) include `expected_hashes.json` entries for `orchestrator_profile` and `broadcaster_profile` but the corresponding `rpc_cache.csv` files were generated before `livepeer-staker profile-backfill` was added (TD-017 closure introduced `profile.rs` to the working tree without regenerating fixtures). Result: `scripts/run-determinism-replay.sh` fails mid-replay with `cache-only replay: missing rpc_call_cache row for eth_call` during the profile-backfill phase. The data layer of TD-017 is correct in production; only the determinism CI fixture is stale. **Compounded by TD-025/TD-026 (2026-05-08):** the two profile tables are now materialized views, so fixture regeneration must also refresh the matviews before re-hashing — `scripts/run-determinism-replay.sh` was updated to issue `REFRESH MATERIALIZED VIEW broadcaster_profile/orchestrator_profile` between replay and hashing, and `docs/DETERMINISM.md` was updated to document the two new source tables (`orch_stake_by_round`, `gateway_balances_by_block`) and the matview semantics. Resolution: do a fresh live profile-backfill against the case-a/case-b block ranges with a clean `rpc_call_cache`, export the resulting cache + hashes, refresh the matviews, and overwrite the committed `expected_hashes.json`. Effort: ~1.5 hours. | TD-022 Phase D 2026-05-08 | low | Open. |
| TD-023 | Rollup checkpoint `updated_at` semantics. Each rollup worker (orch_payouts, orch_rewards, tickets, event_metrics) and `tx_receipts_backfill` early-returned when the candidate query produced zero rows — without calling `advance_checkpoint`. Result: `indexer_checkpoints.updated_at` only ticked when rows were actually written. For sparse-event sources like Reward (which emits ~1×/day on Arbitrum), the checkpoint visibly staled for hours even though the worker was alive and polling correctly. Operators saw "6h stale" in dashboards and treated it as a stalled rollup; the actual situation was "caught up, source quiet." Fix: in the empty-rows path, still call `advance_checkpoint(pg, checkpoint.unwrap_or(0))` — the `GREATEST` clause in the upsert prevents block regression while still bumping `updated_at = now()`. Verified live by restarting all four rollups + tx-receipts-follow: `rollup_orch_rewards_daily` `updated_at` advanced from 10-min-stale to fresh on the next poll while `last_processed_block` and the row count both stayed unchanged (no spurious writes). Profile-follow / gateway-backfill have the same pattern but are about to be touched by TD-022, so deferred to that plan. | rewards-rollup "stall" investigation 2026-05-08 | low | **Resolved** on 2026-05-08. |
| TD-022 | Profile worker RPC batching via Multicall3. `livepeer-staker profile-follow` is structurally RPC-bound: each `NewRound` event fans out 1,936 orchs × ~2 `eth_call`s = ~3,872 RPC reads sequenced through `buffer_unordered(12)`. Plan was to pack calls via Multicall3 for a projected ~100× RPC reduction. Investigation 2026-05-08 surfaced three blockers that collapsed the projected win to ~1.5×: (a) Multicall3's universal CREATE2 deployment (`0xcA11bde0…`) is **not** present on Arbitrum One — verified empirically via `eth_getCode` returning `0x` on both providers; pivoted to JSON-RPC batching (no on-chain dep). (b) Chainstack rate-limits per **constituent** call inside a batch, not per HTTP envelope, so a batch of 100 still triggers RPS limits — had to back off to batch=25/concurrency=2. (c) The per-orch DB queries (`cache::get`, transcoder cuts, lifecycle state) remain sequential and now bound the iteration regardless of RPC batching. Empirical measured speedup with rolled-back-to-25 batch: ~22.8 M blocks/hr vs. OLD's ~15 M/hr → 1.5×, not 100×. **Decision (2026-05-08): rolled back; not landed.** Backfill priority outweighs the modest win. The general-purpose `livepeer_core::rpc::multicall::batch_call_cached` helper introduced for the work is left in place as a future-use utility (zero callers, harmless). Future re-attempt would need: (i) higher Chainstack tier or self-deployed Multicall3 to bypass the per-constituent RPS limit, AND (ii) parallelization of the per-orch DB queries. Plan + investigation notes: [td-022-staker-profile-multicall-batching.md](active/td-022-staker-profile-multicall-batching.md). | profile-follow throughput investigation 2026-05-08 | medium | **Closed (not landed)** on 2026-05-08. |
| TD-020 | Transaction receipt persistence. `/reports/gateway-payouts.csv`, `/reports/payouts.csv`, and `/reports/rewards.csv` computed on-chain transaction fees by fanning out per-tx `eth_getTransactionReceipt` calls at request time, with a serial loop in `load_tx_fees`. Cold-cache exports of larger ranges took 9-24s; the deeper issue was that receipt data was never first-class indexer state. Resolved by adding `tx_receipts` (migration 041) — a `(chain_id, tx_hash)`-keyed table with denormalized block context, gas, fee (wei + eth), status, and from/to. Two new `livepeer-staker` subcommands (`tx-receipts-backfill` and `tx-receipts-follow`) materialize rows from `eth_getTransactionReceipt` via `single_call_cached`, with `buffer_unordered` fan-out and a Phase-E-style metrics surface. The reports endpoint cutover replaces the serial RPC loop with a hybrid SQL+RPC `load_tx_fees` — the SQL path serves from `tx_receipts` and the bounded RPC fallback (concurrency 12) handles any rows not yet backfilled. Backfill executed end-to-end on the live DB: 1,416,505 rows written in ~3 hours at concurrency 8 with no 429s. Verified: April 2026 cold export (~6,378 unique txs) drops from 9-24s to **1.4s** (Index-Only Scan on `tx_receipts_pkey`, byte-identical CSV across reruns). Determinism contract: receipts are a deterministic projection of `rpc_call_cache`, so no replay-manifest hash entry is required (added to `docs/DETERMINISM.md`). Fixture regeneration deferred — `livepeer-orchestrator replay` doesn't invoke tx-receipts-backfill, so existing fixtures stay green. Open follow-up TD-020.5: delete the RPC fallback path in `load_tx_fees` once the table is verifiably populated for ≥ 1 week. Closure details: [td-020-tx-receipts-persistence.md](completed/td-020-tx-receipts-persistence.md). | SQL audit P5 2026-05-08 | medium | **Resolved** on 2026-05-08. |
| TD-028 | CSS hygiene cleanup — frontend audit against [docs/references/modern-css-2026.md](../references/modern-css-2026.md) found rule 1 (Light DOM) fully compliant and rules 2/3 mostly compliant with isolated violations. Three buckets in one PR: (a) 9 inline `style="..."` attributes extracted into component-scoped classes across `dashboard.ts`/`history-list.ts`/`governance-proposals.ts`/`governance-proposal-detail.ts`/`stats-perf.ts`/`data-table.ts`; (b) ~6 semantic-tag swaps (`<div class="card stat">` → `<article>`, card-as-button antipattern in governance-proposals replaced with `<a class="card-link">` wrapping `<article class="prop">`, `<menu>` for history-list row actions); (c) `themes/auto.css` + `themes/dark.css` migrated from hex/rgb to `oklch()` + `color-mix(in oklab, …)` matching `themes/light.css`. `.card-link` promoted from view-dashboard scope to `base.css`. Closure details: [td-028-css-hygiene-cleanup.md](completed/td-028-css-hygiene-cleanup.md). | frontend audit 2026-05-09 | low | **Resolved** on 2026-05-09. |
| TD-029 | CSS modernization sprint — same audit found rule 4 (Modern CSS adherence) was the weakest area despite strong foundations (`@layer`, `@container`, `oklch`, `:where`). Five phases all landed: **Phase 0** Playwright `toHaveScreenshot()` visual regression harness (8 routes, mocked API, fixed viewport, animations disabled) — `tests/visual/routes.spec.ts` + `playwright.config.ts`; **Phase A** native `&` nesting refactor across `base.css`/`components.css` (1000+ lines)/`reset.css`, cap depth 2, `@container` and `@media` queries nested inside their target rules; **Phase B** `light-dark()` adoption in `themes/auto.css` (now actually OS-preference-driven; was a dark-only alias), branded themes untouched; **Phase C** fluid `clamp()`-based spacing tokens at the `_shape.css` token layer (`--sp-6`/`--sp-7`/`--sp-8` and new `--sp-section`/`--sp-page` aliases; body text and micro-spacing stay fixed); **Phase D** `:has()` PoC migrating proposal-outcome tinting (`view-governance-proposals article.prop:has(.pill--pos)`) plus a `.field:has(:user-invalid)` form-validity pattern in `base.css`, alongside `frontend-ui/src/styles/PATTERNS.md` documenting the patterns. Visual harness produced zero-diff against committed baselines through every refactor pass. **Bonus:** `npm audit` 6 → 0 vulnerabilities (`happy-dom` 15→20, `vite` 5→6, `vitest` 2→3) in same session. Closure details: [td-029-css-modernization-sprint.md](completed/td-029-css-modernization-sprint.md). | frontend audit 2026-05-09 | medium | **Resolved** on 2026-05-09. |
| TD-030 | On-chain validation tooling. Comparing the new API to the legacy `livepeer-backend-rs` exposed discrepancies in counts/payouts/gateway names but only tells us the two systems disagree, not which is right. The chain itself is the only authoritative source for current-state validation: every `orchestrator_profile` row corresponds to `BondingManager.{transcoderTotalStake,getTranscoder}` reads, every `broadcaster_profile` row to `TicketBroker.getSenderInfo`. Plan: **Phase A** `scripts/validate-vs-onchain.sh` (~4 h) — bash + curl + cast/python sample-and-diff against prod, configurable N, exit-code semantics; **Phase B** `livepeer-orchestrator validate` Rust subcommand (~8 h) reusing existing on-chain readers from `livepeer-staker::profile`/`gateway`, structured JSON output, `--fail-on-drift`, runs as docker tools-profile one-shot or cron; **Phase C** CI nightly job (~30 min) with Telegram alerting on drift. Out of scope (future TDs): historical validation via `getStakeAtRound`, Livepeer subgraph cross-check (`api.thegraph.com/subgraphs/name/livepeer/arbitrum-one`), validation of derived data (`orch_payouts_daily`, `event_valuations`) which require replay or third-party indexer cross-check. Plan: [td-030-onchain-validation-tooling.md](active/td-030-onchain-validation-tooling.md). | post-TD-027 deploy validation gap 2026-05-10 | medium | Open. |

## Resolved

| ID | Item | Resolved by | When |
|---|---|---|---|
| Q-OD-1 | LPT precision — SQLite REAL is lossy at last 2–3 of 18 LPT decimals; re-derive `amount_native` from RPC, take `*_usd` and `*_price` from SQLite. | [sqlite-seed-mapping.md](../design-docs/sqlite-seed-mapping.md) | 2026-04-27 |
| Q-OD-2 | `transaction_id` is unique in both `payout` (297,105) and `reward` (158,448) — no `log_index` disambiguation needed. | [sqlite-seed-mapping.md](../design-docs/sqlite-seed-mapping.md) | 2026-04-27 |
| Q-OD-3 | `events.payload` is denormalized JSON with consistent envelope; v1 ignores it (price-overlay, not event-mirror). Optional v1.5 cross-check (TD-004). | [sqlite-seed-mapping.md](../design-docs/sqlite-seed-mapping.md) | 2026-04-27 |
| Q-OD-4 | `block_cursors` not consumed (SPEC v1.2 simplification) — the valuator does a flat `(tx_hash, asset)` seed lookup; no per-type bound vector. The SQLite→on-chain naming bridge (e.g. `WinningTicket` → `WinningTicketRedeemed`) is still needed by the cross-check pass (TD-004). | [sqlite-seed-mapping.md](../design-docs/sqlite-seed-mapping.md) | 2026-04-27 |
| Q-OD-5 | RPC topology — no self-hosted Nitro; secondary is hosted liveinfraspe (non-archive). SPEC v1.3 §13.1 corrected. | [on-chain-references.md](../design-docs/on-chain-references.md) | 2026-04-27 |
| Q-OD-6 | Existing API (`livepeer-backend-rs`) — Axum 0.7 + SQLite + ethers; port 4000; CryptoCompare-priced; no inbound auth. We're replacing it, not bolting onto it. | SPEC §14 (v1.2 — API surface) | 2026-04-27 |
| Q-OD-7 | BondingManager event field layouts: `Bond.additionalAmount` (NOT `bondedAmount`) is per-event LPT inflow; full mapping table for all stake-flow events. | [bonding-manager-event-fields.md](../design-docs/bonding-manager-event-fields.md) | 2026-04-27 |
| Q-OD-8 | Chainlink ETH/USD aggregator on Arbitrum = `0x639Fe6ab55C921f74e7fac1ee960C0B6293ba612`; verified decimals=8, description="ETH / USD", `answeredInRound==roundId`. | [on-chain-references.md](../design-docs/on-chain-references.md) | 2026-04-27 |
| Q-OD-9 | LPT/WETH pool cardinality: 1–2 from pool deployment through block ~33M; jumped to 601 in (33M, 35M]. **~17,032 monetary events in `[genesis, 32M)` are not in the seed and must take a degraded valuation version**. v1 must build the degraded path. Earliest Livepeer event = block 6,072,093 (2022-02-15). | [on-chain-references.md](../design-docs/on-chain-references.md) | 2026-04-27 |
| Q-OD-10 | L2 Sequencer Uptime Feed on Arbitrum = `0xFdB631F5EE196F0ed6FAa767959853A9F217697D`; verified decimals=0, description="L2 Sequencer Uptime Status Feed", currently UP. | [on-chain-references.md](../design-docs/on-chain-references.md) | 2026-04-27 |
| TD-007 | Valuator's on-chain Chainlink + Uniswap reads also UPSERT into `token_prices_by_block` (5 callsites in `crates/livepeer-valuator/src/onchain.rs`: ETH/USD via Chainlink; LPT/USD + LPT/WETH for both degraded-spot and TWAP paths). `/prices/{asset}/{quote}/{block,latest}` serves the populated rows; live verified at $2.194 LPT/USD. | [v1-impl.md progress log](active/v1-impl.md) | 2026-04-27 |
| TD-001 | `BondingVotes` ABI not vendored — confirmed by user 2026-04-28 that no such ABI exists for the proxy at `0x0B9C25...`. Closed; the address-only entry in `config/arbitrum.yaml` stays for reference but is not fetched at boot. SPEC §6.7 governance events are served entirely by `LivepeerGovernor.json`. | user confirmation | 2026-04-28 |
